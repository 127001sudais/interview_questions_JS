// Javascript has both strict and type-converting comparisons

// Strict comparison (e.g ===) checks for value equality  without allowing different types
// Abstract comparison (e.g ==) check for value equality only

let a = "32"
let b = 32

console.log(a === b)
console.log(a == b)